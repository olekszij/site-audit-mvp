{
  "name": "site-audit-mvp",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:css",
    "build:css": "npx @tailwindcss/cli -i ./public/src/styles.css -o ./public/styles.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./public/src/styles.css -o ./public/styles.css --watch",
    "test": "node --check app.js && node --check server.js && node --check audit.js && node --check netlify/functions/app.js && npm run build:css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.18.1",
    "cheerio": "^1.2.0",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
